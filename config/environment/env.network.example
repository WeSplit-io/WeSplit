# Network Configuration Example
# Copy relevant sections to your .env.development or .env.production file

# ============================================================================
# NETWORK CONFIGURATION (REQUIRED)
# ============================================================================

# Primary network selection (recommended approach)
# Values: 'devnet' | 'mainnet' | 'testnet'
# - Development: Set to 'devnet' (or omit, defaults to devnet)
# - Production: Set to 'mainnet' (required, or defaults to mainnet in production builds)
EXPO_PUBLIC_NETWORK=devnet

# Legacy network configuration (still supported for backward compatibility)
# EXPO_PUBLIC_DEV_NETWORK=devnet  # 'devnet' for development, 'mainnet' for production
# EXPO_PUBLIC_FORCE_MAINNET=false  # Set to 'true' to force mainnet

# ============================================================================
# RPC PROVIDER API KEYS (OPTIONAL but RECOMMENDED for mainnet)
# ============================================================================

# For best mainnet performance, configure at least one RPC provider with API key
# Priority order: Alchemy > GetBlock > QuickNode > Chainstack > Helius

# Alchemy - Best free tier (30M CU/month), sub-50ms latency (RECOMMENDED)
# Get free API key at: https://www.alchemy.com/
EXPO_PUBLIC_ALCHEMY_API_KEY=your_alchemy_api_key_here

# GetBlock - Fastest (16-21ms latency) (RECOMMENDED)
# Get free API key at: https://getblock.io/
EXPO_PUBLIC_GETBLOCK_API_KEY=your_getblock_api_key_here

# QuickNode - Most reliable (99.99% uptime, 72ms latency) (RECOMMENDED)
# Get free endpoint at: https://www.quiknode.com/
# Format: https://YOUR-ENDPOINT.solana-mainnet.quiknode.pro/YOUR-API-KEY/
EXPO_PUBLIC_QUICKNODE_ENDPOINT=your_quicknode_endpoint_here

# Chainstack - Good balance (low latency, enterprise-grade)
# Get free endpoint at: https://chainstack.com/
# Format: https://YOUR-ENDPOINT.solana-mainnet.chainstack.com/YOUR-API-KEY
EXPO_PUBLIC_CHAINSTACK_ENDPOINT=your_chainstack_endpoint_here

# Helius - Good performance with API key
# Get free API key at: https://www.helius.dev/
EXPO_PUBLIC_HELIUS_API_KEY=your_helius_api_key_here

# ============================================================================
# BACKEND NETWORK CONFIGURATION (Firebase Functions)
# ============================================================================

# Backend must match client network for transaction signing
# Set in services/firebase-functions/.env
# SOLANA_NETWORK=devnet  # Must match EXPO_PUBLIC_NETWORK

# Backend RPC API keys (if different from client)
# HELIUS_API_KEY=your_helius_api_key_here
# ALCHEMY_API_KEY=your_alchemy_api_key_here

# ============================================================================
# NOTES
# ============================================================================

# Production Safety:
# - Production builds automatically default to mainnet if EXPO_PUBLIC_NETWORK is not set
# - Devnet override is disabled in production builds
# - Network validation prevents mismatches

# Development:
# - Development builds default to devnet if EXPO_PUBLIC_NETWORK is not set
# - Runtime network override available in dev builds only (requires app restart)

# See NETWORK_CONFIGURATION.md for complete documentation

